annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output
application.title=simpledb
application.vendor=juno
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# Os arquivos em build.classes.dir que devem ser exclu\u00eddos do jar de distribui\u00e7\u00e3o
dist.archive.excludes=
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/simpledb.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.powercoder.jar=lib/powercoder.jar
file.reference.simdate.jar=lib/simdate.jar
file.reference.xstream-1.4.3.jar=lib/xstream-1.4.3.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.xstream-1.4.3.jar}:\
    ${file.reference.simdate.jar}:\
    ${reference.shellparser.jar}:\
    ${reference.reflector.jar}:\
    ${reference.json-io.jar}:\
    ${reference.checker.jar}:\
    ${reference.remote-objects.jar}:\
    ${reference.slogV2.jar}:\
    ${reference.streams.jar}:\
    ${reference.http-utils.jar}:\
    ${reference.concurrent.jar}:\
    ${file.reference.powercoder.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.processorpath=\
    ${javac.classpath}
javac.source=1.8
javac.target=1.8
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javac.test.processorpath=\
    ${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=us.pserver.sdb.main.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
project.checker=../../../../../storage/java/checker
project.concurrent=../../../../../storage/java/concurrent
project.http-utils=../../../../../storage/java/http-utils
project.json-io=../../../../../storage/java/json-io
project.reflector=../reflector
project.remote-objects=../../../../../storage/java/remote-objects
project.shellparser=../shellparser
project.slogV2=../../../../../storage/java/slogV2
project.streams=../../../../../storage/java/streams
reference.checker.jar=${project.checker}/dist/checker.jar
reference.concurrent.jar=${project.concurrent}/dist/concurrent.jar
reference.http-utils.jar=${project.http-utils}/dist/http-utils.jar
reference.json-io.jar=${project.json-io}/dist/json-io.jar
reference.reflector.jar=${project.reflector}/dist/reflector.jar
reference.remote-objects.jar=${project.remote-objects}/dist/remote-objects-1.0.jar
reference.shellparser.jar=${project.shellparser}/dist/shellparser.jar
reference.slogV2.jar=${project.slogV2}/dist/slogV2.jar
reference.streams.jar=${project.streams}/dist/streams.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project.
# You may also define separate properties like run-sys-prop.name=value instead of -Dname=value.
# To set system properties for unit tests define test-sys-prop.name=value:
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
